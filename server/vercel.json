{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "cd .. && npm install --include=dev",
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "www",
  "functions": {
    "api/index.mjs": {
      "includeFiles": "{vendor/neondatabase-serverless/**,node_modules/@neondatabase/serverless/**,node_modules/drizzle-orm/node_modules/@neondatabase/serverless/**}"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/uploads/(.*)", "destination": "/api" },
    { "source": "/((?!api/|uploads/).*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "no-store" }]
    }
  ]
}
